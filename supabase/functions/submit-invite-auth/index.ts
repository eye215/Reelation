// Stable authenticated endpoint used by the web client. The implementation
// lives in one place so validation, privacy and transaction rules cannot drift
// between legacy and current function names.
import '../submit-invite/index.ts';
